<div id="burger_area">

	<ul id="undevoured">
		{{#each burgers}}
			{{#unless this.devoured }}
				<li>
					{{ burger_name }}
					<button class='devour-btn' 
						data-id={{this.id}}>
						<i 
							class="fa fa-cutlery" 
							aria-hidden="true"></i>
					</button>
				</li>
			{{/unless}}
		{{/each}}
	</ul>

	<ul id="devoured">
		{{#each burgers}}
			{{#if this.devoured }}
				<li>
					<s>{{ burger_name }}</s>
					<button class='devoured-btn'>
						<i 
								class="fa fa-times" 
								aria-hidden="true"
								data-id={{this.id}}></i>
					</button>
				</li>
			{{/if}}
		{{/each}}
	</ul>

</div>